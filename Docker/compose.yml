services:

  marimo:
    image: abmhamdi/nlp
    ports:
      - "1357:1357"
    volumes:
      - ../Codes/.venv/share:/data
    command: python -m marimo edit --host 0.0.0.0 --port 1357 --headless --no-token
